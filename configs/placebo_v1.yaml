# Placebo-caption ladder (scripts/124). Internal; VT condition only (scripts/61 --conds VT).
dm:
  split: [0.6, 0.2, 0.2]
model:
  name: Qwen/Qwen2.5-VL-3B-Instruct
paths:
  data_root: data
  dm_dir: data/dm/placebo_v1
  dm_images: data/dm/images
  runs: runs
seed: 0
