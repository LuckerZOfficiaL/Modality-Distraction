# Candidate text-source pre-test (scripts/120). Not a released artifact.
dm:
  split: [0.6, 0.2, 0.2]
model:
  name: Qwen/Qwen2.5-VL-3B-Instruct
paths:
  data_root: data
  dm_dir: data/dm/textsrc_pretest
  dm_images: data/dm/images
  runs: runs
seed: 0
