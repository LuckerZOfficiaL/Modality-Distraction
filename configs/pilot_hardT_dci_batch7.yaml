seed: 0

paths:
  data_root: data
  dm_dir: data/dm/sonnet_hardT/batch7
  dm_images: data/dm/images
  cc3m_dir: data/pretraining/cc3m_subset
  runs: runs

seeds:
  source: dci
  num_seeds: 7000
  captions_per_image: 1
  hard_t: true   # tightened C1-C7 rules
  t_only: true   # T-only generation (skip V MCQ)
  # NEW: two-stage bridge + per-seed file mode (sidesteps shell-quoting bugs)

model:
  name: Qwen/Qwen2.5-VL-3B-Instruct

sae:
  sparsity: topk
  k: 32
  width_mult: 8

dm:
  target_per_class: 100
  split: [0.6, 0.2, 0.2]
