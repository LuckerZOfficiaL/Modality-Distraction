seed: 0

paths:
  data_root: data
  dm_dir: data/dm/roco/batch500
  dm_images: data/dm/roco/images
  cc3m_dir: data/pretraining/cc3m_subset
  runs: runs

seeds:
  source: dci      # generic-photo prompt; no roco-specific prompt yet
  num_seeds: 500
  captions_per_image: 1
  hard_t: false

model:
  name: Qwen/Qwen2.5-VL-3B-Instruct

sae:
  sparsity: topk
  k: 32
  width_mult: 8

dm:
  target_per_class: 100
  split: [0.6, 0.2, 0.2]
